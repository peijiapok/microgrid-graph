# Progress — 2026-06-16 session (non-Fanchen unblocking work)

Context: ADR-0001 set continuity as the primary objective and committed to Path A (flow constraints in v1). This session did everything on the critical path that does **not** depend on Fanchen's C1/C3 design.

## Done
1. **Feeder split frozen (ADR-0002 + config).** Validated all candidate feeders with `scripts/validate_feeders_v1.py`. Findings: every candidate is operationally **radial** (case33bw tie-lines are `in_service=False`; MVLV-urban radializes via 15 open switches), and **100% of lines carry `max_i_ka`** → `F_e` available for flow constraints. Frozen split written to `configs/topology_split_v1.yaml`:
   - G_train: case33bw (32), 1-LV-rural2 (99), 1-LV-rural3 (118)
   - G_ood: 1-LV-urban6 (111, feeder_type-gen), 1-MVLV-urban-5.303 (242, size-gen), 1-MVLV-urban-6.305 (249)
   - Resolves `docs/11` blocker #1; retires the IEEE 123/34 OpenDSS conversion risk (blocker #3) to v2.
2. **Continuity metric spec (`notes/continuity_metric_spec.md`).** Capacity-normalized, priority-weighted, windowed continuity + oracle denominator `B*` + anti-gaming diagnostics. Oracle injected via callable so the flow-constrained version drops in unchanged.
3. **`src/sg_resilience/metrics_v1.py` implemented** — served indicator (outage-conditioned), windowed continuity, capacity normalization, greedy global-budget oracle, coverage/fairness diagnostics, adequacy guard, transfer gap, rollout bundle.
4. **`tests/test_metrics_v1.py`** — 6 tests (discrimination, anti-gaming, oracle normalization, outage-conditioning, vectorization-vs-reference, bundle smoke). **All pass.** Clears `docs/11` blocker #5 (metric tests) partially (projection tests still pending).

## Done (cont.) — flow-constraint data foundation
5. **`src/sg_resilience/topology.py`** — `build_radial_tree(net)` honoring line `in_service` + switch states, exposing per-edge thermal capacity `F_e` (line: `sqrt(3)·Vn·Imax`; trafo: `sn_mva`), downstream-load membership, and `branch_flows` / `flow_violations` for the constraint `|f_e(a)|≤F_e`. This is the data foundation for `Δ_grid`; it is the correctness oracle Fanchen's *differentiable* projection (C3) must match, not the projection itself.
6. **`tests/test_topology.py`** — 4 tests on a deterministic 3-edge chain (structure, downstream flow accumulation, capacity + violation, non-radial rejection). All pass. Full suite now **10/10**.
7. **Validated on real feeders** (`build_radial_tree` over the frozen split): radiality holds, root edges cover all loads, SimBench caps are realistic (LV 0.19–0.63 MW; MV trafo 63 MW) and bind near actual demand.

### FINDING — case33bw line ratings are non-physical
`max_i_ka` on case33bw yields `F_e ≈ 2.2×10⁶ MW` (pandapower placeholder). So **flow constraints will NOT bind on case33bw** — its topology won't matter under `Δ_grid`. Options for the ADR/sync: (a) assign physical conductor ratings to case33bw, (b) keep it but rely on the SimBench feeders for the topology-binding story, or (c) tighten budget so caps bind. The SimBench feeders are unaffected.

## Done (cont.) — full non-Fanchen runway built & tested (20/20 tests pass)
8. **Loader correctness fix** — `benchmark_loader._build_network_graph` now delegates to `topology.active_bus_graph`, honoring `in_service`/switch states. Verified: MVLV-urban-5.303 now loads radial (was +11 meshed).
9. **`src/sg_resilience/flow_projection.py`** — reference (non-differentiable) Euclidean projection onto `Δ_grid` via cvxpy/OSQP (budget+box+outage+flow caps), and `make_flow_oracle(tree, critical_ids)` — a flow-aware greedy oracle matching the `metrics_v1` oracle signature. This is the correctness oracle Fanchen's differentiable C3 projection must match; it does NOT pre-empt his design. (`tests/test_flow_projection.py`, 4 tests.)
10. **`src/sg_resilience/outages.py`** — per-node two-state Markov outage process (real node ids, persistence so continuity is policy-controllable). Empirically validated rate + run-length; persistence beats i.i.d. (`tests/test_outages_and_loss.py`).
11. **`src/sg_resilience/continuity_loss.py`** — differentiable soft windowed-continuity surrogate (sigmoid soft-served → log-cumsum windowed product), outage-transparent. Gradient + monotonicity tested. This is the training driver (distinct from the metric and from `λ_s` anti-chatter).
12. **End-to-end integration smoke** (real feeder, case33bw with tightened caps): tree → Markov outages → per-step `Δ_grid` projection (feasible at every step: flow+budget+box+outage) → `metrics_v1` with the flow oracle. Under 80%-budget scarcity: C≈0.47, critical coverage 1/3, starvation 0.60 — correct shedding behavior. Aligned `flow_violations` tol with protocol `epsilon_feas=1e-6`.

**Test suite: 20 passing** (metrics 6, topology 4, flow 4, outages+loss 6). Files: `tests/test_{metrics_v1,topology,flow_projection,outages_and_loss}.py`.

## Concrete follow-ups surfaced (Jia-side, non-Fanchen)
- **Loader bug:** `benchmark_loader._build_network_graph` ignores `line.in_service` and open switches → builds the wrong (meshed) graph. Must honor switch states + add a radiality assertion before flow constraints are correct (ADR-0002 §4.1).
- Loader must expose **edge capacity `F_e`** (line `max_i_ka`→MW, trafo `sn_mva`) and a **root-oriented tree** (from ext_grid slack) for subtree sums.
- **Workload audit** (docs/05 §9): MVLV-urban load scale is ~150× the rural feeders; confirm normalized demand/budget distributions are comparable or reframe those OOD feeders as mixed topology/scale shift.

## Scope correction — only a NARROW slice is truly Fanchen-gated
The feasible set `Δ_grid` is *defined* in ADR-0001; only the *fast differentiable* projection is open. Buildable now WITHOUT him (reference/eval implementations that become his correctness oracle):
- reference (non-differentiable, cvxpy) projection onto `Δ_grid` using `topology.py`;
- flow-constrained oracle for the metric denominator (injects into `metrics_v1`);
- scenario/outage generator with Markov persistence (non-edge-correlated part);
- continuity loss term as a standalone PyTorch function;
- the loader `_build_network_graph` switch-state fix (item below).

Truly blocked on Fanchen:
- the **differentiable training projection (C3)** algorithm — radial/laminar subtree-sum vs generic OptNet QP;
- the **flow-aware MP operator (C1)**;
- edge-correlated outage process design;
- ADR-0002 §5 trafo-edge handling confirmation.

## Docs to rewrite after Fanchen sync (deferred deliberately)
`docs/01, 02, 05, 10` — continuity-primary metric, flow-constrained feasible set, re-pin `Delta_graph`/`Delta_transfer` thresholds on `C`. Not touched yet to avoid rewriting twice.
